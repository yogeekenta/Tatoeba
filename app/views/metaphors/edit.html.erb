<div class="contents row">
    <%= form_tag("/situations/#{@situation.id}/metaphors/#{@metaphor.id}", method: :patch ) do %>
      <h3>
        編集する
      </h3>
      <input placeholder="Image Url" type="text" name="image" value="<%= @metaphor.image %>" autofocus="true">
      <textarea cols="30" name="text" placeholder="text" rows="10"><%= @metaphor.text %></textarea>
      <input type="submit" value="SENT">
    <% end %>
  </div>

<!-- /situations/#{@situation.id}/metaphors/#{@metaphor.id} -->
